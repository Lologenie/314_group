Звіт

Це є програмне забезпечення виконане з метою практичного відпрацювання завдань з прикладної криптології.
_____________________________________________________________________________________________________________________________________________________________
Завдання 1 - https://github.com/DartanyanSID2/vigilant-train
Даний код має клас Main, який містить два методи: filterTextWithSpaceAlphabet  , filterTextNormalAlphabet та main.

1)Метод filterTextWithSpaceAlphabet:

Приймає рядок тексту text в якості вхідного параметра.
Вилучає всі символи, які не є буквами чи пробілами з вхідного тексту. Для цього використовується метод replaceAll з регулярним виразом [^a-zA-Zа-яА-ЯіІїЇєЄґҐ ], який видаляє всі символи, крім літер алфавіту та пробілів.
Замінює послідовності пробілів на один пробіл. Це робиться за допомогою використання методу replaceAll з регулярним виразом \\s+.
Видаляє пробіли на початку та в кінці тексту за допомогою методу trim.
Перетворює всі символи на малі літери за допомогою методу toLowerCase.
Повертає оброблений текст.

2)Метод filterTextNormalAlphabet:

Приймає рядок тексту text в якості вхідного параметра.
Вилучає всі символи, які не є буквами з вхідного тексту. Використовується метод replaceAll з регулярним виразом [^a-zA-Zа-яА-ЯіІїЇєЄґҐ], який видаляє всі символи, крім літер алфавіту.
Перетворює всі символи на малі літери за допомогою методу toLowerCase.
Повертає оброблений текст._____________________________________________________________________________________________________________________________________________________________
Завдання 2 Pz_1.2 містить одразу і 3 завдання  - https://github.com/DartanyanSID2/vigilant-train
Основний клас TextAnalyzer  містить метод main і 5  методів :  
TextAnalyzer(String text):

1)Метод конструктора, який ініціалізує TextAnalyzerоб’єкт наданим текстом.
Він приймає Stringпараметр text, що представляє текст, який потрібно проаналізувати.
Встановлює для textполя об’єкта TextAnalyzerнаданий текст.
TextAnalyzer(File file) throws FileNotFoundException:

2)Метод конструктора, який ініціалізує TextAnalyzerоб’єкт шляхом читання тексту з файлу.
Він приймає Fileпараметр file, що представляє файл, з якого читається текст.
Читає вміст файлу за допомогою a Scannerта додає кожен рядок до StringBuilder.
Встановлює textполе об’єкта TextAnalyzerдо вмісту файлу.
Викидає, FileNotFoundExceptionякщо файл не знайдено.
static TextAnalyzer createFromKeyboard():

3)Статичний фабричний метод, який створює TextAnalyzerоб’єкт шляхом читання тексту з клавіатури.
Пропонує користувачеві ввести текст через консоль за допомогою System.in.
Читає введений текст за допомогою a Scannerта зберігає його у Stringзмінній.
Повертає новий TextAnalyzerоб’єкт, ініціалізований введеним текстом.
static TextAnalyzer createFromFile(File file) throws FileNotFoundException:

4)Статичний фабричний метод, який створює TextAnalyzerоб’єкт шляхом читання тексту з файлу.
Приймає Fileпараметр file, що представляє файл, з якого читається текст.
Намагається створити TextAnalyzerоб’єкт шляхом передачі файлу конструктору.
Викидає, FileNotFoundExceptionякщо файл не знайдено.
Map<Character, Double> calculateSymbolFrequenciesWithSpaces():

5)Обчислює частоту кожного символу в тексті, включаючи пробіли.
Ініціалізує a HashMap<Character, Integer>для збереження кількості кожного символу.
Перебирає кожен символ у тексті, оновлюючи кількість кожного символу на карті.
Обчислює частоту кожного символу шляхом ділення його кількості на загальну кількість символів.
Повертає a HashMap<Character, Double>, що містить частоти символів_____________________________________________________________________________________________________________________________________________________________

Курсант 314 навчальної групи 
солдат Сідненко Дмитро Віталійович 
01.04.2024
